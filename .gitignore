# MacOS DS_Store files
.DS_Store

# Gradle cache folder
.gradle

# Gradle build folder
build

# IntelliJ
out/
.idea
*.iml
# mpeltonen/sbt-idea plugin
.idea_modules/

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*

# Common working directory
run

# The game writes its logs beside its working directory, so launching one from this folder leaves them here
logs

# ...and its config folder as well: running 26.2 from the repository root leaves a config/ here
# (anchored: only the top-level one is the game's)
/config/

# Downloaded OptiFine test jar (do not redistribute)
test-downloads/

# Local reference snapshot of the upstream sources
reference/

# Local build/packaging output
dist/
build-log.txt
*.log

# release/publish.ps1 生成的提交载荷,只是留档用
release/tmp/
